# Untitled 002

Imported from the supplied untitled 002 vignette; the original folder is untouched. Preserves the procedural geometry, bundled legacy Three.js, soundtrack, analysis data, and credits.

Click or tap the scene to start audio. Thumbnail mode skips audio and freezes a deterministic frame.

Rendering repairs: apply DPR once; size depth and composer targets to the drawing buffer; release composer targets on resize; clear packed depth to far depth; correct the DOF texel uniform and case-sensitive shader URL.
